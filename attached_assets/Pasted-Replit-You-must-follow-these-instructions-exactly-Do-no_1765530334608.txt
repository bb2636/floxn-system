Replit에게 전달할 지시문 (그대로 붙여넣기)

You must follow these instructions exactly. Do not guess. Do not change requirements.
We have a critical data integrity issue in the estimate module (플록슨).

Context (Tabs)

We have these tabs that must be consistent and persist correctly:

복구면적 산출표

노무비

자재비

견적서

Additionally, 견적서 탭의 데이터는:

현장출동보고서의 “견적서 영역”과 완전히 동일하게 연동되어야 하고

종합진행관리의 “견적금액”에도 동일한 금액으로 노출되어야 합니다.

Current Problem (Repro)

When I input data, the UI linkage looks correct.
But after I press “Save” (저장) and reload:

복구면적 산출표에서 공종 / 공사명 값이 저장 후 사라짐(날아감)

자재비 탭에 내가 추가하지 않은 항목들이 저장 후 대량으로 자동 추가됨

견적서 탭의 견적서 금액과 현장출동보고서의 견적서 금액이 일치하지 않음

더 심각하게, 견적서 탭과 현장출동보고서의 견적서가 컬럼 구조도 다르고 금액도 다름 (must be identical)

종합진행관리의 ‘견적금액’도 견적서에서 작성한 금액과 다른 금액이 표시됨

Requirements (Non-negotiable)

A) Save 이후에도 복구면적 산출표의 공종/공사명 포함 모든 필수 값이 영구 저장되어야 함.
B) 자재비는 사용자가 추가한 것만 저장되어야 하며, 저장 후 임의/중복/대량 자동 생성이 발생하면 안 됨.
C) 견적서 탭의 견적서 데이터(컬럼 + 금액 + 항목 구성)는 현장출동보고서 견적서와 1:1 동일해야 함.
D) 종합진행관리의 ‘견적금액’은 **견적서 탭에서 확정된 최종 견적 금액(단일 source of truth)**을 그대로 표시해야 함.
E) “입력 시엔 정상인데 저장 후 깨짐”은 보통 저장 로직/정규화/ID 매핑/중복 upsert/merge 로직 문제이므로, 저장/로드 전체를 점검해야 함.

What you must do (Step-by-step)

Identify the single source of truth table/model for “Estimate” and all related rows (area sheet, labor, material, estimate summary).

Trace the full save pipeline (UI state → API → DB write) and the full load pipeline (DB read → API → UI).

Add detailed logs ONLY for debugging (then keep them behind a debug flag):

Before save: payload summary (counts + key IDs)

After save: DB write result (created/updated rows count, IDs)

After load: returned counts + IDs

Fix these likely root causes (do not skip investigation):

Wrong overwrite: save request missing fields so DB update sets them null (공종/공사명 날아감)

Accidental “sync all” behavior: material auto-population runs on save/load with incorrect conditions

Duplicate insertion: upsert key not stable (e.g., missing unique key per estimate/material row)

Mismatched schema: estimate tab and dispatch report are reading from different tables or different mapping layer

Total calculation: 종합진행관리 uses a different calculation path than 견적서 탭

After fix, enforce invariants:

The estimate tab and dispatch report must read from the same model OR a guaranteed identical projection.

Materials must not be created unless explicitly requested by user action.

Validation (You must provide evidence)

Provide a reproducible test: create estimate with X rows in each tab → save → reload → confirm:

복구면적 산출표 공종/공사명 retained

자재비 row count unchanged unless user added

견적서 tab JSON == 현장출동보고서 견적서 JSON (same columns, same values)

종합진행관리 견적금액 == 견적서 최종금액

Share the exact endpoint(s) and DB tables involved and show the mapping.

Do NOT refactor unrelated parts. Do NOT change DB schema unless absolutely necessary; if you think it is necessary, explain why and propose minimal migration.