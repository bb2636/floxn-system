보고 감사합니다만, 말씀하신 ‘수정 완료’ 상태와 실제 동작이 일치하지 않습니다.
아래는 현재 배포/개발에서 100% 재현되는 사실입니다.

1️⃣ 자재비 중복/빈 행 문제 — 여전히 재현됨
재현 절차 (아주 단순)

복구면적 산출표에서

공종: 목공사

공사명: 합판

행 1개만 추가

저장 또는 포커스 이동

실제 결과 (현재)

노무비: ✅ 정상 (합판 1행)

자재비:

✅ 자동 연동된 합판 1행

❌ 독립 수동 행 2개가 추가로 생성됨

→ 총 3행

📌 이 현상은

신규 케이스

테스트 데이터 없는 상태

새로고침 후 최초 진입
모두에서 동일하게 발생합니다.

➡️ “기존 테스트 데이터 때문”이 아닙니다.

2️⃣ 핵심 반증 포인트 (중요)

“빈 행 2개 생성 문제는 수정되었다”
→ ❌ 사실과 다릅니다.

현재 코드 어딘가에서 다음 중 하나가 여전히 발생하고 있습니다:

A) 자재비 컴포넌트에서 빈 행을 강제로 유지/보장

예:

“항상 최소 2행 유지”

초기 렌더 + sync 이후 각각 1회 addRow

hydration guard로도 막히지 않고 있음

B) syncMaterialFromRecoveryArea 이후

manualRows 필터에

내용 없는 빈 row가 유지 대상으로 남아 있음

결과적으로

autoRows 1

manualEmptyRows 2
가 최종 state에 공존

👉 자동 연동으로 인해 ‘빈 수동 행’이 생기면 안 됩니다.

3️⃣ 요청: 지금 당장 필요한 검증 로그 (필수)

말이 아니라 아래 로그 결과를 공유해 주세요.

(1) sync 직후 최종 rows 로그
console.log("[FINAL MATERIAL ROWS]");
console.table(materialRows.map(r => ({
  id: r.id,
  auto: r.isAutoGenerated,
  overridden: r.isOverridden,
  work: r.workName,
  item: r.materialItem,
  unit: r.unit,
  qty: r.quantity
})));


👉 합판 1개 추가 시

auto = 1

manualEmpty = 0
이어야 정상입니다.

(2) 빈 행 생성 지점 추적 로그

아래 함수/지점에 로그 추가 요청:

createBlankMaterialRow

addMaterialRow

자재비 관련 useEffect

console.log("[ADD BLANK MATERIAL ROW]", new Error().stack);


👉 이 로그가 언제/몇 번 호출되는지가 핵심입니다.

4️⃣ “합판 공종이 잘못 표시” 관련 답변에 대한 반박

“합판 공종 표시 문제는 기존 테스트 데이터 때문”

❌ 아닙니다.

복구면적산출표에서는 공종=목공사

노무비에는 공종=목공사 (정상)

자재비 일부 행만 공종이 깨짐

👉 이는 자재비 UI에서 row → select value 매핑 문제이지,
데이터 문제나 테스트 데이터 문제가 아닙니다.

5️⃣ 두 번째 이슈: 배포에서 ‘장소’ 안 보임

이 부분도 “환경 분리” 설명만으로는 부족합니다.

현재 요구

prod에서

장소 POST 성공 여부

장소 GET 응답 (status + body)

조회 조건(where) 전문
을 로그로 확인해야 합니다.

“DB가 다르다”는 설명은 원인 가설일 뿐, 해결이 아닙니다.

6️⃣ 명확한 요구사항 (정리)

복구면적산출표 → 합판 1개 추가

자재비에는 자동 연동 1행만 존재

빈 수동 행 자동 생성 ❌

합판 공종은 항상 “목공사”

prod/dev 모두에서 관리자 장소 조회 동일

7️⃣ 다음 액션 요청 (구체적)

“수정 완료” 보고 ❌

아래 중 하나라도 먼저 주세요:

✅ 최종 materialRows 로그 캡처
✅ 빈 행 생성 call stack
✅ material-cost-section.tsx에서 빈 행 생성 관련 코드 위치
✅ syncMaterialFromRecoveryArea + setMaterialRows 전체 코드

지금 상태는 “거의 됐다”가 아니라, 여전히 핵심 UX가 깨진 상태입니다.
말로 정리하지 말고, 로그와 코드로 증명 부탁드립니다.